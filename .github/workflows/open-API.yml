name: Workflow Iter

on:
  workflow_call:
    inputs:
      GH_TOKEN:
        required: true
        type: string
    secrets:
      SNYK_TOKEN:
        required: true
      SONAR_TOKEN:
        required: true
      SONAR_HOST_URL:
        required: true

env:
  registry: ghcr.io
  image_name: ${{ github.repository }}

jobs:
  security:
    name: Snyk Scan api
    runs-on: ubuntu-latest
    environment: API
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
      - name: Run Security Checks
        uses: snyk/actions/golang@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: monitor
          args: --file=open-api/go.mod

  tests:
    runs-on: ubuntu-latest
    environment: API
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26'

      - name: Test open api
        run: cd ./open-api && go test ./... -v

  quality:
    name: Analyze api code with SonarQube
    runs-on: ubuntu-latest
    environment: API
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v6
        with:
          go-version: '1.26'
      - uses: 1arp/create-a-file-action@0.4.6
        with:
          path: ./open-api/output/tests/
          isAbsolutePath: false
          file: 'coverage.out'
      - name: Test coverage open api
        run: cd ./open-api && go test -coverprofile ./output/tests/coverage.out ./...
      - uses: SonarSource/sonarqube-scan-action@v8
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

  build-and-push:
    runs-on: ubuntu-latest
    needs: [security, tests]
    environment: API
    permissions:
      contents: read
      packages: write
    steps:
      - name: Get repo
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.registry }}
          username: ${{ github.actor }}
          password: ${{ inputs.GH_TOKEN }}

      - name: PrepareReg Names # Pour virer les majuscule de con
        run: |
          echo IMAGE_REPOSITORY=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]') >> $GITHUB_ENV
          echo IMAGE_TAG=$(echo ${{ github.ref }} | tr '[:upper:]' '[:lower:]' | awk '{split($0,a,"/"); print a[3]}') >> $GITHUB_ENV

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./open-api
          push: true
          platforms: linux/amd64 # ,linux/arm64 : build sur plusieurs plateformes prend un temps fou en GH-actions (+9 minutes)
          tags: ${{ env.registry }}/${{ env.IMAGE_REPOSITORY }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.registry }}/${{ env.IMAGE_REPOSITORY }}:latest
          format: table
          exit-code: '1' # fait échouer le job si vulnérabilité trouvée
          severity: CRITICAL,HIGH
          ignore-unfixed: true # ignore les failles sans correctif dispo
